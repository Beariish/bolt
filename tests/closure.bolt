import print from core

let prefix = "Message printer:"

export fn(message: any) {
    return fn { print(prefix, message) }
} as message_printer

export fn(max: number) {
    let i = 0
    return fn: number? {
        //i += 1
        if i < max { return i }
        return null
    }
} as range