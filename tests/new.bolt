import * from core
import * from closure

for outer in range(10) {
    let start_time = time()
    
    for i in range(10000000) {
    }
    
    let end_time = time()
    
    let duration_ms = end_time - start_time
    print("10m range() iterations took", duration_ms / 1000, "ms")
}