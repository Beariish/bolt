import * from "../test"

push_scope("literals")

test("numbers", fn {
    let a = 0
    let b = 1000
    let c = -10
    let d = 0.125
    let e = -0.125
    let f = 1.
})

test("strings", fn {
    let a = "this is a single line string"
    let b = "this is a string         with a large gap"
    let c = "this is a 
    multiline string"
})

test("booleans", fn {
    let t = true
    let f = false
})

test("null", fn {
    let n = null
})

test("empty array", fn {
    let a = []
})

test("untyped arrays", fn {
    let a = [1, 2, 3, 4]
    let b = ["a", "b", "c"]
    let c = [true, false]
    let d = [null]
})

test("mixed-type arrays", fn {
    let a = [1, "b", false, null]
    let b = ["b", true, null, 2]
})

test("empty set-type arrays", fn {
    let a = [: number]
    let b = [: string]
    let c = [: bool]
    let d = [: null]
})

test("set-type arrays", fn {
    let a = [1, 2, 3 : number]
    let b = ["a", "b", "c" : string]
    let c = [true, false : bool]
    let d = [null : null]
})

test("union-type arrays", fn {
    let a = [1, "a" : number | string]
    let b = ["b", false : string | bool]
})

test("arrays with trailing comma", fn {
    let a = [1, 2, 3, 4, ]
    let b = ["a", "b", "c", ]
})

test("typed array with trailing comma", fn {
    let a = [1, 2, 3, 4, : number]
    let b = ["1", "2", "3", : string]
})

test("table literals", fn {
    let a = { a: 10 }
    let b = { a: "hello" }
    let c = { a: false }
    let d = { a: null }
})

test("nested array in table literal", fn {
    let a = { a: [] }
    let b = { a: [1, 2, 3] }
    let c = { a: [1, 2, 3 : number] }
    let d = { a: [: number] }
})

test("nested table in table literal", fn {
    let a = { a: {} }
    let b = { a: { a: 10 } }
    let c = { a: { a: [] } }
    let d = { a: { a: {} } }
})

test("table literals with non-text keys", fn {
    let a = { 1: 10 }
    let b = { false: 20 }
    let c = { "hello": 30 }
})

test("multi-key table literals", fn {
    let a = {
        a: 10,
        b: false,
        c: "string",
        d: null,
        e: [1, 2, 3],
        f: {
            a: 10,
            b: false,
            c: "string",
            d: null,
            e: [1, 2, 3]
        }
    }
})

test("table literal with trailing comma", fn {
    let a = {
        a: 10,
    }
})

type Test = {
    a: string,
    b: number,
}

test("strongly-typed table literal", fn {
    let a = Test => {
        a: "hello",
        b: 100
    }
})

type UnsealedTest = unsealed {
    a: string,
    b: number,
}

test("unsealed typed table literal", fn {
    let a = UnsealedTest => {
        a: "hello",
        b: 100,
        c: false
    }

    expect(typeof(a.a) == type(string), "Expected `a.a` to be `string`")
    expect(typeof(a.b) == type(number), "Expected `a.b` to be `number`")
    expect(typeof(a.c) == type(any), "Expected `a.c` to be `any`")
})

test("dictionary table literal", fn {
    let a: { ..string: string } = {
        "hello": "world",
        "goodbye": "sailor"
    }

    let b: { ..number: string } = {
        1: "one",
        2: "two"
    }
})

pop_scope()