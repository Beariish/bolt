import * from "test"

push_scope("root")
push_scope("parser")
push_scope("arithmetic")

test("add", fn {
    expect(10 + 10 == 20, "Addition works")
})

test("mul", fn {
    expect(10 * 10 == 20, "Multiplication works")
})

pop_scope()
pop_scope()
pop_scope()