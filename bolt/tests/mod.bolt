import other_mod

let adder = fn(x: number, y: number): number {
    return x + y
}

export adder
export other_mod.constant as constant

export fn(x: number, y: number): number {
    return x - y
} as subber